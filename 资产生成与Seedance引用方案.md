# 资产生成与 Seedance 引用方案

## 目标

从原始剧本提取人物、人物造型、场景和道具，生成资产图片，并将图片注册到 Seedance 资产库。生成视频时，通过 `@资产名称` 引用对应资产图片，保证人物、场景和道具的一致性。

## 整体流程

```text
原始剧本
  ↓
提取资产并创建占位
  ↓
生成资产图片提示词
  ↓
生成资产图片并选择默认图
  ↓
注册到 Seedance 资产库
  ↓
为片段绑定所需资产
  ↓
在视频提示词中使用 @资产名称
  ↓
提交视频生成任务（提示词 + 资产图片）
```

## 1. 从剧本生成资产占位

人物资产采用“全局人物母档案 + 分批造型识别”的两阶段流程，不能仅将剧本按批拆解后机械合并。

### 1.1 全局识别人物母档案

先将完整剧本提交给 AI，对全剧进行一次全局人物识别。此步骤负责建立统一的人物身份，不识别具体服装造型。

每个人物母档案至少保存：

- 统一姓名、别名和称谓
- 主角/配角属性
- 性别、视觉年龄、身份、职业
- 人物关系
- 核心性格、性格变化及关键剧情节点
- 稳定的脸型、五官、肤色、发型、体型
- 固定识别特征
- 全剧出场集数
- 高清正脸图片提示词

人物性格必须参与全局理解，但图片提示词只保留可视觉化的信息，例如将“警觉、克制、有压迫感”转化为眼神、眉形、嘴角状态和面部气质。

人物母档案的图片仅作为身份基准正脸图，不是 Seedance 最终使用的造型图。正脸图要求：

- 高清正面头肩像或胸像
- 中性或符合人物基础气质的轻微表情
- 五官无遮挡、光照均匀、背景简洁
- 不佩戴非固定的帽子、墨镜等配饰
- 不强调某一集的剧情服装
- 尽可能详细描述稳定的脸部识别特征

### 1.2 每 5 集识别人物造型及其他局部资产

完成全局人物识别后，再每 5 集进行一次局部资产识别。每批请求都必须携带全局人物母档案，以统一人物身份和别名。

人物造型主要识别：

- 上装、下装、鞋、面料、颜色和版型
- 发型、妆容的阶段性变化
- 帽子、眼镜、首饰等配饰
- 受伤、血迹、绷带、污渍等剧情状态
- 季节、场合、伪装和身份变化
- 该造型实际出现的集数

普通表情变化不单独创建造型；服装更换、明显妆容变化、戴帽、受伤、伪装等影响视觉连续性的变化应建立独立造型。

分批结果合并时必须：

1. 使用人物母档案统一姓名和别名。
2. 合并相同造型，避免跨批次重复创建。
3. 为每个造型独立汇总实际出现集数，不能直接继承人物的全部出场集数。
4. 区分人物固定特征和阶段性造型特征。
5. 将造型作为子资产，通过 `parent_id` 挂到人物母资产下。

示例：

```text
主角
├── 商务西装：第 1、2、5 集
├── 居家休闲服：第 3、4 集
├── 戴帽潜行造型：第 6 集
└── 受伤后的西装造型：第 8、9 集
```

其他资产包括：

- 场景：赵德厚家中-卧室
- 道具：搪瓷便盆（带“尿盆”字样）

每个资产保存：

```json
{
  "id": "look_jiang_funing_injured_suit",
  "name": "江芙凝",
  "state": "受伤后的西装造型",
  "type": "character_styling",
  "parent_id": "character_jiang_funing",
  "aliases": [],
  "source_locations": ["第8集", "第9集"],
  "status": "placeholder"
}
```

创建后执行别名合并和去重，避免同一资产重复生成。

## 2. 生成资产图片提示词

不要让 Seed 2.1 Pro 一次生成全部资产提示词。

采用分步、分批生成：

1. 按资产类型分组。
2. 每批处理少量资产，建议 1～5 个。
3. 每个资产只提供相关剧本片段、全局美术风格和结构化输出要求。
4. 生成失败或不合格时，只重试当前资产。

不同资产使用不同模板：

### 人物

人物提示词根据全局人物母档案生成，只用于生成身份基准正脸图。提示词必须尽可能详细地描述年龄、脸型、五官比例、眉眼、鼻型、唇形、肤色、发际线、基础发型、体型、可视觉化气质、身份特征和固定识别特征，不应混入某一集的临时服装或受伤状态。

### 人物造型

人物造型必须绑定人物母资产，包含服装、鞋、配饰、发型、妆容、季节、使用场景、受伤等剧情状态和准确出场集数。

造型图片提示词应要求生成同一张角色设定板中的 2～3 个视图：

1. 正面全身视图。
2. 侧面或四分之三全身视图。
3. 高清正脸特写。

所有视图必须是同一个人，服装完整且一致，不出现多人、重复肢体或不同造型混杂。正脸特写必须与人物母资产的默认正脸图保持一致。

### 场景

包含年代、地域、空间结构、家具布局、材质、色调、生活痕迹和固定标志物。

### 道具

包含尺寸、形状、材质、颜色、文字、磨损程度和使用方式。

## 3. 生成和管理资产图片

### 3.1 全局美术风格继承

所有资产图片提示词和生图任务必须自动读取项目全局设置，至少包括：

- `overall_style`：整体美术风格
- `photorealistic`：是否仿真人
- `era_background`：年代背景
- `overall_tone`：整体色调
- `lighting_tone`：光影基调

项目设置优先于默认提示词，不能在未读取项目设置时默认使用“写实影视概念设计”。例如项目为国漫且 `photorealistic=false` 时，人物正脸、人物造型、场景和道具都必须保持统一的国漫美术语言，并明确避免真人摄影、写实皮肤毛孔和摄影棚照片感。

前端传入的临时风格只能作为补充，不能覆盖或绕过项目全局设置。

### 3.2 人物正脸与造型图的生成顺序

人物图片必须按父子依赖顺序生成：

```text
人物母档案
  ↓
生成并确认高清正脸默认图
  ↓
以正脸默认图作为真实参考图
  ↓
生成该人物的 2～3 视图造型设定板
  ↓
选择造型默认图供 Seedance 使用
```

造型图不能仅在文字提示词中写“保持同一个人”。生图接口必须真实传入人物母资产的默认正脸图，以锁定脸型、五官比例、肤色和人物身份，只允许改变服装、发型、配饰、妆容和剧情状态。

父角色尚未生成或确认默认正脸图时，应阻止其子造型直接生图，并提示先完成人物正脸图。重新选择人物默认正脸图后，应标记其现有造型图可能需要重新生成。

Seedance 实际生成视频时优先引用人物造型图；人物正脸图主要用于生成和维护不同造型之间的身份一致性。

### 3.3 通用图片管理

每个资产支持：

- 生成多张候选图片
- 重新生成
- 上传本地图片
- 选择默认图片
- 查看生成任务与失败原因
- 保存图片生成提示词和负面提示词
- 保存生图使用的父参考图及版本关系

资产只有选定默认图片后，状态才变为 `ready`。

```json
{
  "id": "prop_enamel_chamber_pot",
  "name": "搪瓷便盆（带“尿盆”字样）",
  "type": "prop",
  "status": "ready",
  "image_prompt": "...",
  "default_image_url": "...",
  "seedance_asset_id": "..."
}
```

## 4. 注册到 Seedance 资产库

默认图片确认后，将图片上传或注册到 Seedance 资产库，并保存返回的资产标识。

需要维护以下对应关系：

```text
本地资产 ID
  ↔ Seedance 资产 ID
  ↔ @资产名称
  ↔ 默认资产图片
```

资产名称必须唯一且稳定。修改名称时，需要同步更新片段绑定和 Seedance 资产库记录。

## 5. 为片段绑定资产

每个视频片段保存独立的资产绑定：

```json
{
  "episode": 1,
  "fragment_index": 1,
  "assets": [
    {"asset_id": "scene_zhao_dehou_bedroom", "role": "scene"},
    {"asset_id": "look_jiang_funing_spring", "role": "character"},
    {"asset_id": "character_zhao_dehou", "role": "character"},
    {"asset_id": "prop_enamel_chamber_pot", "role": "prop"}
  ]
}
```

资产可以由系统根据剧本自动匹配，用户可手动增删和替换。

## 6. 生成 Seedance 视频

提交任务时包含两部分：

1. Seedance 视频提示词
2. 当前片段绑定的资产图片或 Seedance 资产 ID

提示词使用 `@资产名称` 说明图片对应的对象：

```text
白天的 @赵德厚家中-卧室 场景中，
@【主角】江芙凝-春季日常朴素便装 对应江芙凝，
@赵德厚 对应赵德厚，
@赵大强 对应赵大强，
@赵小栋 对应赵小栋，
@搪瓷便盆（带“尿盆”字样） 对应画面中的搪瓷便盆。
```

`@` 引用由程序根据片段资产绑定自动生成，不依赖模型自由填写。

## 7. 生成前校验

提交视频任务前检查：

- 片段引用的资产是否存在
- 资产是否已有默认图片
- 是否已注册到 Seedance 资产库
- `@资产名称` 是否与注册名称一致
- 人物是否绑定了正确造型
- 是否存在重复或冲突资产
- 引用数量是否超过 Seedance 限制

校验失败时阻止提交，并列出缺失资产。

## 实施重点

1. 资产提取与资产图片提示词生成分开执行。
2. 先用完整剧本建立全局人物母档案和详细正脸提示词，再每 5 集识别局部人物造型。
3. 每批局部识别都携带全局人物母档案，造型独立记录并汇总实际出场集数。
4. 人物与造型建立真实父子关系：人物是父资产，造型通过 `parent_id` 挂载。
5. 图片提示词按类型、按 1～5 个资产的小批次生成，但小批次生成不替代全局人物理解。
6. 所有资产提示词和图片自动继承项目全局美术风格，禁止写实/国漫等风格错配。
7. 先生成并确认人物高清正脸图，再以该图作为真实参考图生成包含高清正脸的 2～3 视图造型图。
8. Seedance 实际引用人物造型图，人物正脸图用于维持不同造型之间的身份一致性。
9. 资产图片选定后注册到 Seedance 资产库。
10. 每个片段保存明确的资产绑定关系。
11. 视频生成时同时提交提示词和绑定的资产图片。
12. `@资产名称` 由程序自动编译，确保名称和图片一一对应。

## 人物资产验收标准

- 完整剧本中的主要人物均有且只有一个人物母档案。
- 人物姓名、别名、身份、关系和核心性格在全剧范围内一致。
- 人物正脸提示词足够详细，不混入临时造型信息。
- 同一人物的所有造型均正确挂在对应人物下。
- 每个造型的出场集数来自该造型实际出现的剧集，而非人物全部出场集数。
- 造型图包含 2～3 个一致视图，并含可用的高清正脸。
- 造型生图真实使用父人物默认正脸图作为参考图。
- 国漫、动画、写实等资产风格与项目全局设置一致。
- Seedance 片段绑定优先选择对应集数和剧情状态的人物造型图。
