# SeedanceMini v2 功能调整与跟进记录

> 本文档用于记录本轮产品调整、已解决问题、当前交互规则与后续跟进事项。后续修改相关功能时，应优先核对本文档、`PLAN.md` 及实际代码。

## 一、本轮调整范围

本轮主要涉及以下模块：

1. 快剪实时预览与裁剪后播放范围。
2. 快剪视频选择历史。
3. 项目详情中的剧集切换。
4. Seedance 视频生成页面的片段与提示词数据同步。
5. Seedance 提示词解析、展示与重新生成。
6. 局域网访问方式。

## 二、快剪功能规则

### 2.1 入点和出点实时预览

- 编辑入点时，左侧视频预览应实时定位到当前入点。
- 编辑出点时，左侧视频预览应实时定位到当前出点。
- 用户完成“删除所选时间段”后，预览只在剩余的有效时间轴范围内循环播放。
- 删除视频前半部分后，播放起点必须自动落到剩余时间段的第一个有效时间点，不能继续尝试播放已经删除的第一帧。

### 2.2 已处理异常

- 修复快剪鼠标操作引用未定义 `setIsSeeking` 导致的报错。
- 修复删除视频前半部分后预览失效或陷入循环的问题。
- 修复点击“删除所选时间段”时因 `setIsSeeking` 未定义而中断的问题。

### 2.3 视频选择历史

- 用户手动上传的视频不记录到快剪选择历史。
- 在快剪视频选择界面选择过的视频也不记录历史。
- 当前产品规则为：快剪选择视频区域不保留选择历史。

## 三、剧集切换规则

### 3.1 入口交互

- 项目详情页“第 N 集”左侧原返回图标改为菜单图标。
- 点击整个剧集标题区域可打开下拉菜单并切换剧集。
- 剧集列表从当前项目剧本中解析，不使用固定占位数据。
- 不展示“第 0 集”，有效剧集从第 1 集开始。

### 3.2 切换联动

切换剧集后，下列区域必须同步到所选集：

- 当前剧集标题。
- 片段列表。
- Seedance 视频生成页面的片段时间线。
- Seedance 提示词内容。
- 当前片段选择状态。
- 页面右侧片段数量。

### 3.3 已处理异常

- 修复 `loadPromptsForEpisode is not defined` 导致项目详情白屏。
- 修复 `projectTab is not defined` 导致项目详情白屏。
- 修复 Seedance 视频生成页面点击剧集后内容没有切换的问题。

## 四、Seedance 视频生成页面规则

### 4.1 片段来源和数量

- 片段时间线必须读取“片段”模块中当前剧集的真实片段。
- 占位数与当前剧集的片段数保持一致。
- 当前剧集没有拆分片段时显示 `0` 个片段。
- 无片段时提示：请在“片段”中先拆分片段。
- 页面标题右侧的片段数量必须与当前剧集实际片段数一致。
- 不允许使用其他剧集的片段数量，也不允许使用固定占位数。

### 4.2 提示词来源

- Seedance 提示词输入框读取“片段”模块为当前片段生成的提示词。
- 当前片段尚未生成提示词时，输入框保持为空。
- 切换剧集或片段时，输入框同步展示对应数据。
- 重新生成成功后，新提示词必须立即写入当前片段对应的数据，并回填输入框，不能只展示生成过程而不保存结果。

### 4.3 空片段安全处理

- 点击片段前必须确认对应片段数据存在。
- 不可读取空对象或 `null` 的 `fragment_index`。
- 已修复点击最后一个片段时 `Cannot read properties of null (reading 'fragment_index')` 导致白屏的问题。

## 五、Seedance 提示词展示规范

### 5.1 字段展示

示例原始字段：

```json
{
  "整体风格": "国漫卡通，写实灾荒质感，线条清晰但人物表情细腻",
  "negative_prompt": "模糊，变形，扭曲",
  "time": "8s",
  "shots": []
}
```

展示规则：

```text
【整体风格】：国漫卡通，写实灾荒质感，线条清晰但人物表情细腻

negative_prompt：模糊，变形，扭曲

镜头1
【景别】：大全景
【运镜】：缓慢推近，低机位横移，节奏压抑

镜头2
……
```

具体要求：

- 中文字段按 `【字段名】：字段值` 展示。
- `shots` 中多个镜头按“镜头1、镜头2……”依次展开。
- 每个镜头内的景别、运镜、动作等字段分别展示。
- `negative_prompt` 的内容必须保留并展示；它是传给视频模型的负向提示词参数，不能因为中文含义是“不要”而被删除。
- 三个资产映射字段当前暂不解析，留待后续统一映射。
- `time` 是当前片段的时长，必须按片段保存，并在生成视频时作为参数传给 Seedance。
- 不展示“【清晰度与比例】”字段。

### 5.2 输入框下方参数

参数使用普通文本展示，不使用标签组件：

```text
8s、720P、9:16 竖屏
```

规则：

- 没有值的参数不显示，例如没有时长时不显示“未设置时长”。
- 视频默认清晰度为 `720P`。
- `9:16` 展示为 `9:16 竖屏`。
- `16:9` 展示为 `16:9 横屏`。
- 横竖屏方向只展示一次，避免出现“9:16 竖屏 竖屏”。

## 六、重新生成提示词

### 6.1 按钮与布局

- 原“重置”按钮改为“重新生成提示词”。
- “重新生成提示词”和“生成视频”按钮均放在提示词区域下方。
- “重新生成提示词”使用弱按钮样式。
- “生成视频”是主操作，视觉优先级更高。

### 6.2 执行逻辑

- 点击“重新生成提示词”等同于在“片段”模块中为当前指定片段生成提示词。
- 复用同一套单片段生成逻辑，避免两个入口行为不一致。
- 当前集应读取已加载提示词数据对应的 `episode`，不能引用未声明的 `currentEpisode`。
- 生成过程中需要实时显示按钮 loading、生成进度和当前片段反馈。
- 生成完成后必须：
  1. 保存新提示词到当前项目、当前剧集和当前片段。
  2. 刷新前端提示词数据。
  3. 将结果立即回填到当前输入框。

### 6.3 已处理异常

- 修复按钮点击无反应的问题。
- 修复生成状态和回调未透传到 `SeedanceVideoStudio` 的问题。
- 修复 `currentEpisode is not defined` 报错。
- 已打通与“片段”模块相同的实时进度反馈链路。

### 6.4 后续重点回归项

“生成后保存并回填输入框”是当前需要持续重点验证的链路。每次修改生成接口、片段索引或剧集切换逻辑后，必须回归以下场景：

1. 切换到任意剧集。
2. 选择其中一个真实片段。
3. 点击“重新生成提示词”。
4. 确认有实时生成反馈。
5. 确认生成完成后输入框立即更新。
6. 切换到其他片段再切回，确认结果仍然存在。
7. 刷新页面，确认结果已持久化而不是仅保存在前端状态中。

## 七、局域网访问

### 7.1 当前配置

Web 服务默认监听：

```text
0.0.0.0:8000
```

涉及位置：

- `web/app.py`
- `start.bat`
- `README.md`

### 7.2 启动方式

macOS 或通用命令：

```bash
python3 -m uvicorn web.app:app --host 0.0.0.0 --port 8000
```

也可直接运行：

```bash
python3 -m web.app --port 8000
```

Windows：

- 启动服务：双击 `start.bat`。
- 停止服务：双击 `stop.bat`。
- `stop.bat` 只会停止监听 `8000` 端口且启动命令包含 `uvicorn web.app:app` 的 Seedance 服务，不会终止占用该端口的其他程序。

### 7.3 同一 Wi-Fi 下访问

在作为服务器的电脑上查询局域网 IP：

```bash
ipconfig getifaddr en0
```

假设输出 `192.168.1.131`，同事访问：

```text
http://192.168.1.131:8000
```

本机仍可访问：

```text
http://127.0.0.1:8000
```

### 7.4 使用条件和注意事项

- 两台设备必须连接同一个 Wi-Fi 或处于可互通的局域网。
- 运行项目的电脑必须保持开机，服务进程不能关闭，电脑不能休眠。
- macOS 或 Windows 防火墙必须允许 Python 接收网络连接。
- 部分公司、酒店和公共 Wi-Fi 会开启客户端隔离，此时同一 Wi-Fi 下也可能无法互相访问。
- 局域网 IP 由路由器分配，重连网络后可能变化；无法访问时应重新查询 IP。
- 所有人访问的是服务器电脑上的同一套项目和数据。
- 当前没有登录与权限隔离，只应在可信局域网内使用。
- 多人同时编辑同一项目可能发生状态覆盖；现阶段建议不同人员使用不同项目，后续如需正式协作应增加用户、权限、并发写入和冲突处理机制。

## 八、数据一致性原则

后续开发应遵守以下原则：

1. 剧集、片段和提示词必须来自项目真实数据，不使用固定占位数据。
2. `episode` 与 `fragment_index` 共同确定唯一片段，切换后必须重新校准当前选择。
3. UI 显示的片段数量、时间线数量和后端实际片段数量必须一致。
4. 提示词重新生成后必须同时更新持久化数据与前端状态。
5. 访问数组元素或片段字段前必须处理空值和越界情况。
6. 快剪删除时间段后，播放头必须移动到剩余有效区间。
7. 前端按钮必须有即时反馈，异步成功后必须明确反映最终结果。

## 九、建议后续事项

- [ ] 完整验证重新生成提示词后的持久化和输入框回填。
- [ ] 为剧集切换、最后一个片段、空片段增加前端回归测试。
- [ ] 为多人同时编辑增加冲突保护或项目级编辑锁。
- [ ] 如需跨网络访问，增加登录鉴权后再考虑正式部署，不能直接暴露当前服务。
- [ ] 统一实现三个资产映射字段的解析及 Seedance `@资产` 映射。
- [ ] 核对 `time` 是否在所有生成入口中均按当前片段传给 Seedance。

## 十、问题反馈建议

后续反馈问题时，建议同时提供：

- 当前项目名称。
- 当前剧集和片段编号。
- 操作步骤。
- 浏览器控制台完整报错。
- 是否刷新页面后仍可复现。
- 对应项目目录下 `shots.json` 和 `seedance_prompts.json` 是否已有该片段数据。
